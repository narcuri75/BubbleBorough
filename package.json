{
  "name": "bubble-borough",
  "private": true,
  "version": "1.0.0",
  "description": "A real-time fish tank idle game.",
  "scripts": {
    "start": "node scripts/start-web.cjs",
    "start:web": "node scripts/start-web.cjs",
    "build:app": "node scripts/build-app-bundle.cjs",
    "check:app": "node scripts/build-app-bundle.cjs --check",
    "check:mobile": "node scripts/check-mobile-patch.cjs",
    "check:notifications": "node scripts/check-notification-consolidation.cjs",
    "check:architecture": "node scripts/check-action-architecture.cjs",
    "test": "npm run check:app && npm run check:mobile && npm run check:notifications && npm run check:architecture && node --check public/app.js && node --check public/store-facets.js && node --check public/store-variants.js && node --test scripts/feature-regressions.test.cjs scripts/store-care.test.cjs scripts/sprite-sheets.test.cjs",
    "generate:assets": "powershell -ExecutionPolicy Bypass -File scripts/generate-assets.ps1",
    "generate:manifest": "powershell -ExecutionPolicy Bypass -File scripts/generate-asset-manifest.ps1",
    "strip:sprite-json": "node scripts/strip-sprite-json-images.cjs"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "typescript": "5.9.3"
  }
}
